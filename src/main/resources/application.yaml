server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/personal_blog?useSSL=false&serverTimezone=UTC
    username: root
    password: 123456

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
